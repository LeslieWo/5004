public class Power {
    public static void main(String[] args) {
        long result = (long) Math.pow(2, 31);
        System.out.println(result);
    }

    /*
     * Differences between Java and Python versions:
     * 1. Java requires a class and a main method, while Python does not.
     * 2. Java uses static typing, while Python uses dynamic typing.
     * 3. Java programs must be compiled before running, while Python scripts are interpreted.
     */
}

